// Importa el paquete de Flutter que contiene los widgets y elementos
// fundamentales para construir interfaces gráficas.
import 'package:flutter/material.dart';

// Importa la clase App desde el archivo app.dart.
// Esta clase representa la aplicación principal.
import 'package:flutter_timer/app.dart';

// Función principal que se ejecuta al iniciar la aplicación.
// Utiliza runApp para iniciar la app Flutter con el widget raíz `App`.
void main() => runApp(const App());
